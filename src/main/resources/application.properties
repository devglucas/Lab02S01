# Perfil ativo por padrao: dev (H2 em memoria, facil de rodar)
spring.profiles.active=dev

# Nome da aplicacao
spring.application.name=aluguel-carros

# Thymeleaf: desliga cache em dev para refletir mudancas nas paginas
spring.thymeleaf.cache=false
